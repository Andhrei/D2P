{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Add {{ class }}{% endblock %}

{% block body %}
    <div class="form-content form-group">
        <h2>Add a schedule</h2>
        <br>
        {{ form_start(form) }}
            <div class="row">
                <div class="col-md-5 form-group">
                    {{ form_label(form.datalist.client) }}
                    <br>
                    {{ form_widget(form.datalist.client, {'attr' :
                            {
                                'class' : 'form-input',
                                'placeholder' : 'Name'
                            }
                        })
                    }}
                </div>
            </div>
            <div class="row">
                <div class="col-md-5 form-group">
                    {{ form_label(form.datalist.device) }}
                    <br>
                    {{ form_widget(form.datalist.device, {'attr' :
                            {
                                'class' : 'form-input',
                                'placeholder' : 'Name'
                            }
                        })
                    }}
                </div>
            </div>
            <div class="row">
                <div class="col-md-5 form-group">
                    {{ form_label(form.recurrence) }}
                    <br>
                    {{ form_widget(form.recurrence, {'attr' :
                            {
                                'class' : 'form-input',
                                'placeholder' : 'Name'
                            }
                        })
                    }}
                </div>
            </div>

            <div class="row">
                <div class="col-md-2">
                    {{ form_widget(form.save, {'attr' : { 'class' : 'btn btn-primary'}}) }}
                </div>
            </div>
        {{ form_end(form) }}
    </div>

    {#<div class="form-content">#}
        {#<h2>Add Client</h2>#}

        {#{{ form_start(form) }}#}
        {#{{ form_errors(form) }}#}
            {#<div class="form-group">#}
                {#{{ form_widget(form.name, {'attr' :#}
                        {#{#}
                            {#'class' : 'form-input',#}
                            {#'placeholder' : 'Name'#}
                        {#}#}
                    {#})#}
                {#}}#}
            {#</div>#}
            {#<div class="form-group">#}
                {#{{ form_widget(form.hostname, {'attr' :#}
                    {#{#}
                        {#'class' : 'form-input',#}
                        {#'placeholder' : 'Hostname'#}
                    {#}#}
                {#})#}
                {#}}#}
            {#</div>#}

        {#{{ form_widget(form.save, {'attr' : { 'class' : 'btn btn-primary'}}) }}#}
        {#{{ form_end(form) }}#}
            {##}
                {#If you want to control the URL the user#}
                {#is redirected to on success (more details below)#}
                {#<input type="hidden" name="_target_path" value="/account" />#}
            {##}
        {#</form>#}


    {#</div>#}

{% endblock %}
